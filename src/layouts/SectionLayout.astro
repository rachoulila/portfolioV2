---
import BaseLayout from "./BaseLayout.astro";
import Header from "../components/Header.astro";
import { getLangFromUrl, useTranslations } from "../i18n/utils";
import { contact } from "../config/site";

interface Props {
  title: string;
  description: string;
  accent: string; // ex: "var(--accent-design)"
  tagline: string;
}

const { title, description, accent, tagline } = Astro.props;
const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<BaseLayout title={title} description={description}>
  <div style={`--section-accent:${accent}`}>
    <Header showBack />
    <div class="section__hero">
      <p class="wrap section__tagline">{tagline}</p>
    </div>
    <main id="main" class="wrap section">
      <slot />
      <footer class="section__contact">
        <h2>{t("contact.title")}</h2>
        <a href={`mailto:${contact.email}`}>{t("contact.email")} — {contact.email}</a>
      </footer>
    </main>
  </div>
</BaseLayout>

<style>
  .section__hero {
    background: radial-gradient(
      ellipse 70% 100% at 20% 0%,
      color-mix(in srgb, var(--section-accent) 30%, transparent),
      transparent 75%
    );
    padding-block: var(--space-6) var(--space-5);
  }
  .section {
    padding-block: 0 var(--space-6);
  }
  .section__tagline {
    font-family: var(--font-display);
    font-weight: 800;
    letter-spacing: -0.03em;
    font-size: var(--text-display);
    line-height: 0.95;
    max-width: 20ch;
    margin: 0;
    color: var(--fg);
  }
  .section__contact {
    margin-top: var(--space-7);
    padding-top: var(--space-4);
    border-top: var(--rule);
  }
  .section__contact h2 {
    font-size: var(--text-small);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--fg-muted);
    margin-bottom: var(--space-2);
  }
  .section__contact a {
    text-decoration: none;
    font-size: var(--text-h2);
  }
</style>
