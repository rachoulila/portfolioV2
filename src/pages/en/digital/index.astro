---
import { getCollection } from "astro:content";
import SectionLayout from "../../../layouts/SectionLayout.astro";
import FilterableProjects from "../../../components/FilterableProjects.astro";

const projects = (await getCollection("projects", ({ data }) => data.page === "digital")).sort(
  (a, b) => a.data.order - b.data.order || b.data.year - a.data.year
);
---

<SectionLayout
  title="Digital & experiences"
  description="Installations, real-time, interactive devices — projects by Rachel Truchot."
  accent="var(--accent-digital)"
  tagline="Digital & experiences."
>
  <p class="section-note">
    The sliding project side panel is coming in the next step — for now a click opens the
    dedicated page.
  </p>
  <FilterableProjects projects={projects} mode="digital" />
</SectionLayout>

<style>
  .section-note {
    color: var(--fg-muted);
    font-size: var(--text-small);
    margin-bottom: var(--space-4);
  }
</style>
